# Skill: Explore

Propósito:
Recolección rápida de contexto del repositorio para que el Orquestador entienda dónde tocar.

Comportamiento:
- Lectura de archivos, extracción de snippets, identificación de puntos de entrada.
- Priorizar archivos con nombres relevantes (`vision`, `ocr`, `config`, `bot`).
- No realizar modificaciones.

Salida:
- JSON con `files`, `snippets`, `summary`.
