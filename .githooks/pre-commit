#!/bin/sh
#
# pre-commit hook — auto-bump patch version en cada commit.
# Se activa con: git config core.hooksPath .githooks
#
# Incrementa el patch en _version.py y lo staged automáticamente
# como parte del commit en curso.

REPO_ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0

# Solo bump si existe _version.py
[ -f "$REPO_ROOT/_version.py" ] || exit 0

cd "$REPO_ROOT" || exit 0

python bump_version.py patch 2>/dev/null || true

# Re-staging para incluir el bump en el commit
git add _version.py 2>/dev/null || true
