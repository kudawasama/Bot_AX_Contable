#!/bin/sh
#
# pre-commit hook — (desactivado) El versionado ahora es manual.
#
# Ya NO se auto-incrementa la versión en cada commit.
# Para bumpear la versión, ejecutar manualmente:
#   python bump_version.py patch   # bug fixes
#   python bump_version.py minor   # nuevas funcionalidades
#   python bump_version.py major   # cambios grandes/rompientes
#
# Se activa con: git config core.hooksPath .githooks
